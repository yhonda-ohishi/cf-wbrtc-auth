{
  "name": "cf-wbrtc-auth",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types",
    "build:client": "esbuild src/client/index.ts --bundle --format=esm --target=es2020 --outfile=dist/client.js --minify",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@connectrpc/connect": "^2.0.0",
    "@connectrpc/connect-web": "^2.0.0",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.1.11",
    "@cloudflare/workers-types": "^4.20251213.0",
    "esbuild": "^0.24.0",
    "tslib": "^2.8.1",
    "typescript": "^5.6.0",
    "vitest": "^1.3.0",
    "wrangler": "^3.91.0"
  }
}
